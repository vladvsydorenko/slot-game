/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/slot-game',
    // assetPrefix: '/slot-game/',
    // distDir: 'docs'
}

module.exports = nextConfig
